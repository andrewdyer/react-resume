import React from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const TrophyIcon = () => <FontAwesomeIcon className="text-warning" icon={['fas', 'trophy']} />;

export default TrophyIcon;
